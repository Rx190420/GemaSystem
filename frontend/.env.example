VITE_API_BASE_URL=http://localhost:8000/api

# reCAPTCHA v2 site key (public, safe to expose in the frontend bundle). The
# value below is Google's official public TEST key — always renders and always
# passes, zero real protection. Replace with your real site key from
# https://www.google.com/recaptcha/admin/create before going live (must match
# the RECAPTCHA_SECRET_KEY set in the backend .env).
VITE_RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI
